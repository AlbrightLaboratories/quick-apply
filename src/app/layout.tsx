import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Quick Apply - Fast, Free Job Applications",
  description: "Upload your resume once, apply everywhere. Quick Apply eliminates repetitive job applications with one-click apply across all major job boards.",
  keywords: ["job search", "job application", "resume", "career", "employment"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} antialiased`}>
        {children}
      </body>
    </html>
  );
}
